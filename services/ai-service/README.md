# AI Service OCR

FastAPI service for OCR processing of receipt images using PaddleOCR. This service transforms raw OCR output into structured receipt data compatible with the SnapExpense Spring Boot application.

## Features

- **PaddleOCR Integration**: Advanced text extraction from receipt images
- **Structured Data Output**: Transforms raw OCR to structured receipt data
- **AWS S3 Support**: Downloads images from pre-signed URLs
- **Multipart Downloads**: Efficient handling of large image files
- **Spring Boot Compatible**: Output format matches core-service DTOs
- **Docker Support**: Containerized deployment ready

## API Endpoints

### POST /ocr/extract

Extract structured data from receipt image.

**Request:**

```json
{
  "presigned_url": "https://s3.amazonaws.com/bucket/receipt.jpg"
}
```

**Response:**

```json
{
  "merchantName": "Big Bazaar Jaipur",
  "totalAmount": 845.5,
  "receiptDate": "2026-03-03",
  "items": [
    {
      "name": "Rice 5kg",
      "quantity": 1.0,
      "unitPrice": 320.0,
      "totalPrice": 320.0
    }
  ],
  "ocrRawText": "Big Bazaar Jaipur Rice 5kg ₹320.00...",
  "ocrConfidence": 0.921
}
```

### GET /health

Health check endpoint.

**Response:**

```json
{
  "status": "healthy",
  "service": "ai-service",
  "version": "1.0.0"
}
```

## Quick Start

### Using Docker

1. Build the image:

```bash
docker build -t ai-service-ocr .
```

2. Run the container:

```bash
docker run -p 8000:8000 ai-service-ocr
```

### Local Development

1. Install dependencies:

```bash
pip install -r requirements.txt
```

2. Run the service:

```bash
python main.py
```

The service will be available at `http://localhost:8000`

## Environment Variables

- `HOST`: Server host (default: 0.0.0.0)
- `PORT`: Server port (default: 8000)

## Architecture

```
├── main.py              # FastAPI application and endpoints
├── models.py            # Pydantic models matching Spring Boot DTOs
├── downloader.py       # Multipart HTTP Range downloader
├── ocr_service.py      # PaddleOCR integration + parsing logic
├── requirements.txt    # Python dependencies
└── Dockerfile          # Container configuration
```

## OCR Processing

The service processes images through these steps:

1. **Download**: Downloads image from AWS S3 pre-signed URL using HTTP Range headers
2. **Extract**: Uses PaddleOCR to extract text with confidence scores
3. **Parse**: Transforms raw OCR output to structured receipt data
4. **Return**: Returns formatted data compatible with Spring Boot DTOs

## Error Handling

- Returns structured error responses for all failure cases
- Graceful degradation when OCR fails (returns empty structure)
- Automatic cleanup of temporary files
- Comprehensive logging for debugging

## Integration with SnapExpense

This service is designed to integrate seamlessly with the SnapExpense core-service:

- Receives pre-signed URLs generated by core-service
- Returns data in the exact format expected by `OcrExtractedData` DTO
- Handles AWS S3 authentication via URL parameters
- Provides confidence scores for receipt verification workflow

## Dependencies

- FastAPI: Web framework
- PaddleOCR: OCR engine
- httpx: HTTP client for downloads
- Pydantic: Data validation
- uvicorn: ASGI server

## Health Monitoring

The service includes built-in health checks and monitoring:

- `/health` endpoint for service status
- Docker health check configuration
- Comprehensive error logging
- Performance metrics logging
